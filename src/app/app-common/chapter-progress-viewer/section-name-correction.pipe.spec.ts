import { SectionNameCorrectionPipe } from './section-name-correction.pipe';

describe('SectionNameCorrectionPipe', () => {
  it('create an instance', () => {
    const pipe = new SectionNameCorrectionPipe();
    expect(pipe).toBeTruthy();
  });
});
